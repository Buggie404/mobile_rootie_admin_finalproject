package com.veganbeauty.admin.core.base

import androidx.lifecycle.ViewModel

abstract class RootieAdminViewModel : ViewModel() {
    // Shared logic for admin viewmodels (e.g. loading states, common errors)
}
